Instead of C++ template, macro is used to build a type independet 
double-ended queue. This data structure is written in C style coding.
It can contain any type besides void. It is implemented with a circular dynamic array.